---
import Button from "../components/Button.astro";
import Hero3D from "../components/Hero3D";
---

<section
    class="relative min-h-screen flex items-center justify-center overflow-hidden py-20 lg:py-0"
>
    <div class="max-w-7xl mx-auto px-6 relative z-10 w-full">
        <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">
            {/* Left Column: Text Content */}
            <div
                class="flex flex-col items-start text-left justify-center h-full"
            >
                {/* Main Headline */}
                <h1
                    class="text-4xl md:text-5xl lg:text-[3.5rem] font-bold tracking-tight animate-fade-up leading-[1.15]"
                >
                    Every model gives you<br class="hidden md:block" />
                    a different answer.<br />
                    <span
                        class="text-transparent bg-clip-text bg-gradient-to-r from-green-primary to-green-400"
                        >Get the right one.</span
                    >
                </h1>

                {/* Subheadline - the REAL pain */}
                <p
                    class="mt-8 text-text-secondary text-base md:text-lg max-w-xl leading-relaxed animate-fade-up animate-delay-200"
                >
                    Stop copy-pasting prompts across OpenAI, Claude, and Codex.
                    Stop comparing outputs in different tabs.
                    <span class="text-text-primary font-medium">Concilium</span> runs
                    them all in parallel, has them peer-review each other's work,
                    and gives you one validated answer &mdash; in a single interface.
                </p>

                {/* CTAs */}
                <div
                    class="mt-10 flex flex-col sm:flex-row gap-4 animate-fade-up animate-delay-400 w-full sm:w-auto"
                >
                    <Button
                        variant="primary"
                        size="lg"
                        href="#get-started"
                        className="w-full sm:w-auto min-w-[180px] justify-center group relative overflow-hidden shadow-[0_0_20px_rgba(34,197,94,0.3)] hover:shadow-[0_0_30px_rgba(34,197,94,0.5)] transition-shadow"
                    >
                        <span
                            class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300"
                        ></span>
                        <span class="relative font-bold tracking-wide"
                            >Get Started</span
                        >
                    </Button>
                    <Button
                        variant="secondary"
                        size="lg"
                        href="https://github.com/matiasdaloia/concilium"
                        className="w-full sm:w-auto min-w-[180px] justify-center border-white/10 hover:border-white/20 bg-white/5 backdrop-blur-sm"
                    >
                        View Source
                    </Button>
                </div>

                {/* Social proof bar */}
                <div
                    class="mt-12 flex items-center gap-6 animate-fade-up animate-delay-600 border-t border-white/5 pt-6 w-full max-w-lg"
                >
                    <div class="flex items-center gap-2">
                        <div class="flex -space-x-1.5">
                            <div
                                class="w-5 h-5 rounded-full bg-provider-opencode/20 border border-provider-opencode/40 flex items-center justify-center text-[7px] text-provider-opencode font-bold"
                            >
                                O
                            </div>
                            <div
                                class="w-5 h-5 rounded-full bg-provider-codex/20 border border-provider-codex/40 flex items-center justify-center text-[7px] text-provider-codex font-bold"
                            >
                                G
                            </div>
                            <div
                                class="w-5 h-5 rounded-full bg-provider-claude/20 border border-provider-claude/40 flex items-center justify-center text-[7px] text-provider-claude font-bold"
                            >
                                C
                            </div>
                        </div>
                        <span class="text-[11px] text-text-muted"
                            >3 agents, 1 answer</span
                        >
                    </div>
                    <div class="h-4 w-px bg-white/10"></div>
                    <span class="text-[11px] text-text-muted"
                        >Open source &middot; MIT</span
                    >
                    <div class="h-4 w-px bg-white/10"></div>
                    <span class="text-[11px] text-text-muted"
                        >macOS & Linux</span
                    >
                </div>
            </div>

            {/* Right Column: Hero Visual */}
            <div
                class="relative w-full flex justify-center lg:justify-end animate-fade-up animate-delay-400"
            >
                <Hero3D client:load />
            </div>
        </div>
    </div>

    {/* Background Gradients */}
    <div class="absolute inset-0 -z-10 pointer-events-none">
        <div
            class="absolute bottom-0 left-0 w-full h-1/2 bg-gradient-to-t from-bg-page via-bg-page/80 to-transparent"
        >
        </div>
        <div
            class="absolute top-0 right-0 w-[800px] h-[800px] bg-green-primary/[0.03] rounded-full blur-[120px] translate-x-1/2 -translate-y-1/4"
        >
        </div>
    </div>
</section>
